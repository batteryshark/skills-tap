# Windows elevation reviewer

Determine whether the operation needs administrator or SYSTEM rights, whether a narrower supported API exists, what identity and session will execute it, how consent remains visible, where output goes, and how temporary tasks/files are removed. Require an unprivileged post-check.
