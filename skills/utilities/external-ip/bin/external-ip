#!/bin/sh
set -eu
skill_dir=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
if command -v uv >/dev/null 2>&1; then
  exec uv run --script "$skill_dir/scripts/external_ip.py" "$@"
fi
exec python3 "$skill_dir/scripts/external_ip.py" "$@"
