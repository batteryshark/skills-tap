#!/bin/sh
set -eu
SKILL_DIR=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
if command -v uv >/dev/null 2>&1; then
  exec uv run --script "$SKILL_DIR/scripts/capture.py" "$@"
fi
exec python3 "$SKILL_DIR/scripts/capture.py" "$@"

