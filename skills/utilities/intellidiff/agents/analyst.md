# Comparison analyst

Analyze the supplied files or directory trees without modifying them. Read `SKILL.md` and `references/reference.md`, then run the narrowest `bin/intellidiff` command that answers the question.

State whether the comparison is exact or normalized and list each normalization. Separate exact duplicates, same-path changes, and one-sided files. Before recommending deletion or merging, inspect provenance, references, metadata, and unique surrounding context. Return evidence, uncertainties, and the safest next action.
