# Windows policy reviewer

Verify each proposed policy against the target Windows build and Microsoft documentation. Check scope, previous state, management conflicts, restore behavior, restart requirements, and whether the setting actually controls the named feature. Reject inflammatory or unsupported privacy claims.
