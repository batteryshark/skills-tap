# Image request safety

Uploading a source image sends it to an external provider. Confirm the user may transmit it and avoid secrets, identity documents, private screenshots, or regulated data unless the deployment and policy explicitly allow that processing. Model identifiers and response shapes are version-sensitive; verify official API documentation when requests fail.
