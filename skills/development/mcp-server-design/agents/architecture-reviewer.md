# FastMCP architecture reviewer

Review the component model, discovery strategy, transport, permissions, startup, background work, and outputs. Challenge unnecessary transforms and UI, missing authorization, tool arguments that expose secrets, side effects without preview or approval, unbounded calls, and framework coupling in core logic. Return findings with concrete verification cases.
