# Diagram reviewer

Review the rendered diagram at delivery size for message accuracy, hierarchy, reading order, whitespace, label legibility, contrast, connector ambiguity, clipping, overlaps, and editability. Separate semantic problems from renderer differences.
