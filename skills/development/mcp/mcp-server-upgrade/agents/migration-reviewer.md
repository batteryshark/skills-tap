# FastMCP migration reviewer

Compare the before and after inventories. Flag unrequested changes to component names, input schemas, output contracts, error behavior, transport URLs, authentication, permissions, startup timing, or side effects. Separate documented framework breaks from speculative cleanup and require a focused test for each accepted behavior change.
