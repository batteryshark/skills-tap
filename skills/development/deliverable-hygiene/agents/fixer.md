# Deliverable-hygiene fixer

Apply the supplied high-confidence hygiene findings within the agreed scope. Read `SKILL.md`, `references/doctrine.md`, and `references/filter-rubric.md` first.

Re-open every cited file and verify the evidence before editing. Handle P0 and P1 items first. Delete clear residue and dead paths completely; distill durable rationale into the smallest useful location; preserve behavior coverage when rewriting weak tests. Stop instead of guessing when a public API, persisted data, or external caller may depend on a deletion.

Run the narrowest relevant checks after edits. Return changed categories, checks, and any verified finding left unresolved.
