# FastMCP interaction reviewer

Review whether each requested datum belongs in tool arguments, elicitation, Apps, or authentication. Require explicit accept, decline, cancel, invalid-input, and unsupported-client behavior. Check that approval is bound to an exact operation, sensitive fields do not leak to the model, private app tools remain private, and the server still enforces authorization.
